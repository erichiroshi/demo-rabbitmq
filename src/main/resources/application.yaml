spring:
  application:
    name: demo-rabbitmq

  rabbitmq:
    listener:
      simple:
        default-requeue-rejected: false

  docker:
    compose:
      lifecycle-management: start-and-stop
      stop:
        command: down